package {{packageName}}.dto;

import jakarta.validation.constraints.*;
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;
import io.swagger.v3.oas.annotations.media.Schema;

import java.io.Serializable;
import java.time.LocalDateTime;

/**
 * Data Transfer Object for {{entityName}} creation and update requests.
 * <p>
 * This DTO is used for POST and PUT operations. It includes Jakarta Bean Validation
 * annotations to ensure data integrity before reaching the service layer.
 * </p>
 * <p>
 * Customize validation annotations based on your business rules:
 * - @NotNull - field cannot be null
 * - @NotBlank - string cannot be null, empty, or whitespace-only
 * - @Size(min, max) - string/collection size constraints
 * - @Min/@Max - numeric range constraints
 * - @Pattern(regexp) - regex pattern matching
 * - @Email - email format validation
 * - @Past/@Future - date/time constraints
 * - @Positive/@PositiveOrZero - numeric sign constraints
 * </p>
 *
 * @author Generated by DevEx AI Assistant
 * @version 1.0
 */
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
@Schema(description = "Request payload for creating or updating a {{entityName}}")
public class {{className}} implements Serializable {

    private static final long serialVersionUID = 1L;

    {{#each fields}}
    /**
     * The {{name}} of the {{../entityName}}.
     * TODO: Customize validation annotations based on business requirements
     */
    @Schema(description = "{{name}} field", example = "Sample {{name}}"{{#if required}}, required = true{{/if}})
    {{#if required}}
    @NotNull(message = "{{name}} is required")
    {{#if isString}}
    @NotBlank(message = "{{name}} cannot be blank")
    @Size(min = 1, max = 255, message = "{{name}} must be between 1 and 255 characters")
    {{/if}}
    {{/if}}
    private {{type}} {{name}};
    {{/each}}
}
