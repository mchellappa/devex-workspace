package {{packageName}}.dto;

import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.NoArgsConstructor;
import com.fasterxml.jackson.annotation.JsonInclude;
import com.fasterxml.jackson.annotation.JsonFormat;
import io.swagger.v3.oas.annotations.media.Schema;
{{{imports}}}

import java.io.Serializable;
import java.time.LocalDateTime;

/**
 * Data Transfer Object for {{entityName}} responses.
 * <p>
 * This DTO is returned by GET, POST, and PUT operations. It represents the
 * complete state of a {{entityName}} entity including system-managed fields
 * like ID and timestamps.
 * </p>
 * <p>
 * <strong>IMPORTANT:</strong> This template automatically includes the following fields:
 * - id (Long): Primary key, auto-generated
 * - createdAt (LocalDateTime): Creation timestamp
 * - updatedAt (LocalDateTime): Last update timestamp
 * 
 * If your OpenAPI schema includes these fields, you may get compilation errors  
 * about duplicate field definitions. Remove them from your OpenAPI schema's
 * properties section - they are system-managed and should not be in the spec.
 * </p>
 * <p>
 * Jackson annotations control JSON serialization:
 * - @JsonInclude(NON_NULL) - null fields are excluded from JSON response
 * - @JsonFormat - controls date/time serialization format
 * - @JsonProperty - rename fields in JSON (if needed)
 * - @JsonIgnore - exclude sensitive fields (if needed)
 * </p>
 *
 * @author Generated by DevEx AI Assistant
 * @version 1.0
 */
@Data
@Builder
@NoArgsConstructor
@AllArgsConstructor
@JsonInclude(JsonInclude.Include.NON_NULL)
@Schema(description = "Response payload containing {{entityName}} data")
public class {{className}} implements Serializable {

    private static final long serialVersionUID = 1L;

    /**
     * Unique identifier for this {{entityName}}.
     * Assigned by the database on creation.
     */
    @Schema(description = "Unique identifier", example = "1", accessMode = Schema.AccessMode.READ_ONLY)
    private Long id;

    {{#each fields}}
    /**
     * The {{name}} of the {{../entityName}}.
     */
    @Schema(description = "{{name}} field", example = "Sample {{name}}")
    private {{{type}}} {{name}};
    {{/each}}

    /**
     * Timestamp when this {{entityName}} was created.
     * Automatically set by the system.
     */
    @Schema(description = "Timestamp when the entity was created", accessMode = Schema.AccessMode.READ_ONLY)
    @JsonFormat(pattern = "yyyy-MM-dd'T'HH:mm:ss")
    private LocalDateTime createdAt;

    /**
     * Timestamp when this {{entityName}} was last updated.
     * Automatically updated by the system.
     */
    @Schema(description = "Timestamp when the entity was last updated", accessMode = Schema.AccessMode.READ_ONLY)
    @JsonFormat(pattern = "yyyy-MM-dd'T'HH:mm:ss")
    private LocalDateTime updatedAt;
}
